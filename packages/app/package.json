{
  "name": "template-doctor-frontend",
  "version": "0.0.1",
  "description": "Template Doctor frontend",
  "scripts": {
    "dev": "vite",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "start": "python3 -m http.server 8080",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
  "build": "vite build && mkdir -p dist/configs && cp -f configs/*.json dist/configs/ && cp -f config.json dist/ 2>/dev/null || true && if [ -d results ]; then mkdir -p dist/results && rsync -a --exclude '*/node_modules' --exclude '*/.git' results/ dist/results/; fi",
    "typecheck": "tsc -p . --noEmit",
    "pretest": "npm run build"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "@playwright/test": "^1.55.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
